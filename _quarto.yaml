project: 
  render:
    - slides.qmd

format:
  html:
    theme: cosmo
    css: styles.css
    toc: true
    link-external-newwindow: true

# Code on the slides is pulled from scripts/ by this filter, so the deck holds
# no executable chunks and renders without R.
filters:
  - filters/include-code.lua

editor: visual
