# R session files
.Rproj.user
.Rhistory
.RData
.Ruserdata

# Quarto internals and rendered output. CI renders slides.qmd and publishes the
# deck to gh-pages and the PDF to a release, so nothing rendered is tracked.
/.quarto/
/_site/
*.html
*.pdf
*_files/
*_cache/
**/*.quarto_ipynb

# LaTeX build artifacts from the beamer render
*.aux
*.log
*.nav
*.out
*.snm
*.toc
*.vrb
*.fls
*.fdb_latexmk

# Reference papers for the MCMC section: not ours to redistribute
mcmc_papers/

.DS_Store
